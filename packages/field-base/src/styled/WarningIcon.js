import styled from 'styled-components';
import { colorInvalid } from './constants';

const WarningIcon = styled.div`
  align-items: center;
  color: ${colorInvalid};
  display: flex;
`;

export default WarningIcon;
