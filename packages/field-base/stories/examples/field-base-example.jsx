import React from 'react';
import { FieldBaseStateless } from '@atlaskit/field-base';

export default (
  <FieldBaseStateless
    appearance="subtle"
    onBlur={() => {}}
    onFocus={() => {}}
  >
    hover over me to
  </FieldBaseStateless>
);
