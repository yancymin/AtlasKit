import {
  akColorB100,
  akColorB400,
  akColorB500,
  akColorN30,
  akColorN500,
} from '@atlaskit/util-shared-styles';

const paddingVertical = 5;
const paddingHorizontal = 10;
const margin = paddingHorizontal / 2;
const colorLabelDefault = akColorN500;
const colorLineDefault = akColorN30;
const colorSelected = akColorB400;
const colorActive = akColorB500;
const colorHover = colorSelected;
const colorFocus = akColorB100;
const focusWidth = 2;
const lineHeight = 25;
const fontSize = 14;

export {
  colorActive,
  colorFocus,
  colorHover,
  colorLabelDefault,
  colorLineDefault,
  colorSelected,
  focusWidth,
  fontSize,
  lineHeight,
  margin,
  paddingHorizontal,
  paddingVertical,
};
