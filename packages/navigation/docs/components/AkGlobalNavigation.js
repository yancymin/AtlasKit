import React from 'react';

export const description = (
  <div>
    <p>
      The global navigation represents the blue bar to the left. It is rendered by
      default by the default export, however it is exported in case you need to use
      it outside this context.
    </p>
    <p>
      The global navigation renders specific home, search and add items, which are
      custom globalItems at the top of the page, and can render non-standard items
      at the bottom of the navigation.
    </p>
  </div>
);

export const examples = [];

export const byline = 'The global navigation section of navigation by itself.';
