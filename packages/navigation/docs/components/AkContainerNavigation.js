import React from 'react';

export const description = (
  <div>
    <p>
      The primary location for navigation items.This container is designed to
      look after its own positioning. The default export uses this component by
      default, however if you need it outside of that context, it is exported
      separately here.
    </p>
  </div>
);

export const examples = [];

export const byline = 'The primary container in which most items are rendered.';
