import React from 'react';

export const description = (
  <div>
    <p>
      This is a styling and wrapping for items in the container navigation. It has
      its contents set by props, not children. It defaults to being a link. An array
      of navigation items is the most common children for the default export.
    </p>
  </div>
);

export const examples = [];

export const byline = 'Main component to user for rendering navigation items.';
