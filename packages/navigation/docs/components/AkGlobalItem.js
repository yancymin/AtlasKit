import React from 'react';

export const description = (
  <div>
    Wraps items to be used in the global navigation, adding styling and a link.
    The globalPrimaryIcon, globalCreateIcon, and globalSearchIcon will automatically
    be wrapped in a globalItem when provided to the default component. secondaryItems
    are not.
  </div>
);

export const examples = [];

export const byline = 'Item to be rendered in the global navigation, wrapping it in a link.';
