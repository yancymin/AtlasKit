import React from 'react';

export const description = (
  <div>
    A custom implementation of the drawer component designed specifically for use
    with the create global navigation item. It should be passed to the drawers prop,
    and click of the globalCreateIcon does not automatically cause it to render.
  </div>
);

export const examples = [];

export const byline = 'Custom drawer designed to be rendered on click of the globalCreateIcon.';
