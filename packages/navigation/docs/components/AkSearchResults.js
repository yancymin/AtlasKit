import React from 'react';

export const description = (
  <div>
    The container logo component is designed to wrap the logo inside something
    probably because that{"'"}s how this is all set up.
  </div>
);

export const examples = [];

export const byline = 'A logo component designed to be used in the navigation container.';
