import React from 'react';

export const description = (
  <div>
    <p>
      Drawers are designed to be passed as an array to the drawers property of the
      default export. Drawers are either open or closed, controlled through the `isOpen`
      variable. While closed they are hidden. While open they take over the page,
      disabling interaction with other elements.
    </p>
    <p>
      As well as the custom drawer export, there are specific implementations exported
      for use with create or search.
    </p>
    <p>
      It should be passed to the drawers prop.
    </p>
  </div>
);

export const examples = [];

export const byline = 'A standard drawer component for generic use.';
