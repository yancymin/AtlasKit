import React from 'react';

export const description = (
  <div>
    <p>
      Nested Navigation is designed to handle transitions between different
      navigation panels, and animates in new children when they are provided. It
      can be provided to the navigation container, or as the child of the default
      export. Transitions are animated.
    </p>
  </div>
);

export const examples = [];

export const byline = 'Animated transitions between different navigation panels.';
