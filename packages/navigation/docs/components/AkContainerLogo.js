import React from 'react';

export const description = (
  <div>
    <p>
      The container logo component is designed to wrap a logo to appear at the top
      of the container. It should be provided as the first child of the container.
    </p>
    <p>
      This component renders its children with enforced styles to help with positioning.
    </p>
  </div>
);

export const examples = [];

export const byline = 'A logo component designed to be used in the navigation container.';
