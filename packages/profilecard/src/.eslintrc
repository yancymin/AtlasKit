{
  "extends": "@atlaskit/eslint-config-base",
  "settings": {
    // Tell eslint-plugin-compat which browser features we have pollyfilled
    // At this time caniuse (the database that compat uses) does not cover everything.
    "polyfills": [
      "promises",
      "fetch"
    ]
  }
}
